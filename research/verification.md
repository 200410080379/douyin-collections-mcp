# 验证记录

日期：2026-09-15。账号已由用户在独立 Chrome 会话中自行登录。此文件不记录 Cookie、签名、账号 ID 或视频标题。

## 已完成

- 官方 MCP Python SDK stdio 客户端可发现全部 13 个工具。
- 已通过安装到 PATH 的命令，从 `/tmp` 启动服务并调用工具。
- 真实读取：当前账号、收藏、点赞、8 个已有收藏夹、收藏夹内容、单条作品详情。
- 长整数 ID 通过浏览器原始文本 → Python JSON 解析，收藏夹优先使用 `collects_id_str`。
- 读取请求不复用旧的动态签名/令牌参数；连续三次读取收藏夹成功。
- 真实 MCP 方案流程：读取已在收藏夹里的视频，准备方案，执行时识别已存在，返回 `completed`、`already_present=true`、`verified=true`。该测试未改变已有分类。
- 88 项本地自动化测试通过，代码与脚本 Ruff 检查通过。
- MCP 已注册到 Codex，名称 `douyin-collections`；启动超时 30 秒，工具超时 300 秒。

## 后续真实写入验证

用户手动创建私密测试收藏夹后，已完成以下验证：

- `scripts/test_existing_folder.py`：将一条已有收藏加入测试夹，回读确认，重跑已完成方案，移出新增测试条目，确认原收藏与点赞状态未变。测试夹保留为空。
- 新建普通中文名称的私密收藏夹，回读确认 `visibility=private`；空测试夹清理成功。
- `scripts/mcp_live_smoke.py`：从 `/tmp` 启动已安装的 stdio MCP，通过官方 MCP 客户端完成登录检查、创建私密夹、读取视频、生成分类预览、执行入夹、读取夹内容、续跑去重。
- 同一 MCP 验收中读取仅点赞未收藏的视频，预览显式设置 `allow_favorite_liked=true`，实际完成先收藏再入夹。
- 验收后移出测试成员，对原先未收藏的视频恢复未收藏状态，保留其点赞，确认两条测试视频的原收藏和点赞状态均已恢复；自动创建的测试夹已删除。

结构化执行结果记录在本机忽略目录 `output/existing-folder-smoke.json` 和 `output/mcp-live-smoke.json`。这两份结果包含测试对象 ID，不应公开发布。

这些是小范围功能验收，没有自动整理用户的全部收藏。全量任务由用户提供分类意图，再生成和执行计划。

## 早期创建失败记录

新建收藏夹的同源请求、网站 Axios 请求客户端，以及网页原生新建表单，均返回 `status_code=3002184`、`status_msg=操作失败`。原生表单测试已关闭“设置为公开”。未发现已创建的测试收藏夹。

错误原因尚未确认，不能把它解释为账号限制、风控、名称限制或实现问题中的某一种。后续原生表单和实际 MCP 的普通中文名称创建均已通过，该错误不再构成当前验收的阻塞。

上述后续记录已经覆盖新增入夹、仅点赞视频先收藏、新建回读与测试清理；本地模拟测试仍不替代这些真实证据。

`scripts/live_smoke.py` 会真实尝试创建一个临时私密测试夹并加入一条已有收藏；它不是离线测试，不能在不需要写入验证时随意运行。`scripts/capture_create_request.py` 只检查网页构建的请求并在发送前中止，不创建收藏夹。
